import React from "react";

function page() {
  // todo a checklist of people present - could import from somewhere
  // then on form submission the needed people get the link
  // then a redirect to the monitor quiz panel/
  // must be redundant so that 
  return <div>page</div>;

}

export default page;
